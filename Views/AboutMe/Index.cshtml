@model IEnumerable<KurumsalWebProjesi.Models.Model.AboutMe>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}



<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.Raw(item.Description)
        </td>
        <td>
            @Html.ActionLink("Update", "Edit", new { id=item.AboutMeId }) 
            
        </td>
    </tr>
}

</table>
